mixin formatDateTime(time)
    span #{moment(time).format("DD/MM/YYYY H:m:ss")}

mixin formatDate(time)
    span #{moment(item.createdBy.createdAt).format("DD/MM/YYYY")}

mixin formatTime(time)
    span #{moment(time).format("H:m:ss")}